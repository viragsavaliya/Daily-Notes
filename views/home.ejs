<%- include('partials/header.ejs') %>

<% const length = 100; %>

<h1> Home </h1>
<p> <%=homePara%> </p>

<% posts.forEach(function(post) { %>
   <h2> <%= post.title %> </h2>
   <p> <%= post.content.substring(0, length) + "..." %> <a href= "/posts/<%= post.title %>"" > Read More </a>  </p>
<% }); %>

<%- include('partials/footer.ejs') %>
